# Copy this file to .env and fill values (do NOT commit .env)
# MongoDB connection string
MONGODB_URI=

# Port the server should listen on (Render provides PORT automatically)
PORT=5000

# Optional: HOST (0.0.0.0 recommended for Docker)
HOST=0.0.0.0

# NODE_ENV=production
NODE_ENV=production
